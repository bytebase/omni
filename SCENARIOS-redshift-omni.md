# Redshift Omni Scenarios

Goal: replace Bytebase's ANTLR-backed Redshift parser with a hand-written Omni Redshift dialect while preserving the runtime surfaces Bytebase already enables: SQL review, syntax diagnostics, SQL editor validation, masking query span, autocomplete, statement report, and changed-resource extraction.

Reference sources:
- Legacy parser grammar and examples: `github.com/bytebase/parser/redshift` at `v0.0.0-20260417075056-57b6ef7a2640`.
- Official syntax: Amazon Redshift SQL command documentation.
- Existing Bytebase consumers: `backend/plugin/parser/redshift`, `backend/common/engine.go`, SQL Editor, export resources, statement report, and LSP completion.

Progress rules:
- A scenario passes only when a focused automated test proves the behavior.
- Legacy examples that parse today must not regress.
- Unsupported cases must fail explicitly; they must not produce misleading AST, query span, or completion results.

## Phase 0: Scaffold And Corpus

### Package Shape

- [x] `redshift` exposes `Parse(sql string) ([]Statement, error)`.
- [x] `redshift/parser` exposes a hand-written recursive-descent parser forked from Omni PostgreSQL.
- [x] `redshift/ast` contains Redshift AST nodes and walker support.
- [x] `redshift/catalog` can analyze PostgreSQL-compatible Redshift SELECT queries.
- [x] `redshift/completion` exposes completion context for Redshift SQL.
- [x] `go test ./redshift/...` runs without depending on Bytebase.

### Legacy Corpus

- [x] All 115 legacy Redshift example files are available to tests or test fixtures.
- [x] The corpus is classified into P0 parse, P1 parse, and unsupported/deferred groups.
- [x] PostgreSQL-compatible examples from the legacy corpus parse successfully.
- [x] Redshift-specific examples are tracked as expected failures until implemented.
- [x] Corpus failures report the source file and first syntax error.

## Phase 1: Parser Foundation

### Statement Boundaries

- [x] `Split` separates top-level semicolon-delimited statements.
- [x] `Split` ignores semicolons inside single-quoted strings.
- [x] `Split` ignores semicolons inside double-quoted identifiers.
- [x] `Split` ignores semicolons inside dollar-quoted strings.
- [x] `Split` ignores semicolons inside block comments.
- [x] `Split` handles Redshift procedure/function blocks without splitting internal statements.

### Core Statements

- [x] `SELECT 1` parses as a select statement.
- [x] `WITH c AS (SELECT 1) SELECT * FROM c` parses with a CTE.
- [x] `INSERT INTO t SELECT 1` parses as DML.
- [x] `UPDATE t SET a = 1 WHERE id = 2` parses as DML.
- [x] `DELETE FROM t WHERE id = 1` parses as DML.
- [x] `MERGE INTO target USING source ON target.id = source.id WHEN MATCHED THEN UPDATE SET v = source.v` parses as DML.
- [x] `MERGE ... REMOVE DUPLICATES` parses as Redshift-specific MERGE.
- [x] `EXPLAIN SELECT 1` parses as utility SQL.
- [x] `EXPLAIN ANALYZE SELECT 1` parses and is classified distinctly from plain explain.

## Phase 2: Redshift DDL And Utility Syntax

### CREATE TABLE

- [x] `CREATE TABLE t (id INT)` parses.
- [x] `CREATE TABLE t (id INT IDENTITY(1,1))` parses and records identity options.
- [x] `CREATE TABLE t (id INT GENERATED BY DEFAULT AS IDENTITY)` parses.
- [x] `CREATE TABLE t (name VARCHAR(32) ENCODE lzo)` parses and records column encoding.
- [x] `CREATE TABLE t (id INT DISTKEY)` parses and records column distkey.
- [x] `CREATE TABLE t (id INT SORTKEY)` parses and records column sortkey.
- [x] `CREATE TABLE t (id INT) DISTSTYLE KEY DISTKEY(id)` parses and records table distribution options.
- [x] `CREATE TABLE t (id INT) SORTKEY(id)` parses and records compound sort key.
- [x] `CREATE TABLE t (id INT) INTERLEAVED SORTKEY(id)` parses and records interleaved sort key.
- [x] `CREATE TABLE t (id INT) ENCODE AUTO` parses and records automatic encoding.
- [x] `CREATE TABLE t (id INT) BACKUP NO` parses and records backup behavior.

### Data Movement

- [x] `COPY t FROM 's3://bucket/file' IAM_ROLE DEFAULT` parses.
- [x] `COPY t FROM 's3://bucket/file' IAM_ROLE 'arn:aws:iam::1:role/r' FORMAT AS CSV` parses.
- [x] `COPY t FROM 's3://bucket/file' CREDENTIALS 'aws_access_key_id=...'` parses.
- [x] `UNLOAD ('SELECT * FROM t') TO 's3://bucket/out' IAM_ROLE DEFAULT` parses.
- [x] `UNLOAD` options parse for `FORMAT`, `MANIFEST`, `HEADER`, `DELIMITER`, `ENCRYPTED`, and compression.

### SHOW And DESC

- [x] `SHOW DATABASES` parses.
- [x] `SHOW DATABASES LIKE 'dev%' LIMIT 10` parses.
- [x] `SHOW SCHEMAS FROM DATABASE dev LIMIT 10` parses.
- [x] `SHOW TABLES FROM SCHEMA dev.public LIKE 't%' LIMIT 10` parses.
- [x] `SHOW COLUMNS FROM TABLE public.t` parses.
- [x] `SHOW GRANTS ON TABLE public.t` parses.
- [x] `SHOW DATASHARES` parses.
- [x] `DESC DATASHARE share_name` parses.

### Redshift Objects

- [x] `CREATE DATASHARE s` parses.
- [x] `ALTER DATASHARE s ADD TABLE public.t` parses.
- [x] `DROP DATASHARE IF EXISTS s` parses.
- [x] `CREATE EXTERNAL SCHEMA s FROM DATA CATALOG DATABASE 'db' REGION 'us-east-1' IAM_ROLE DEFAULT` parses.
- [x] `CREATE EXTERNAL TABLE s.t (id INT) STORED AS PARQUET LOCATION 's3://bucket/'` parses.
- [x] `CREATE EXTERNAL VIEW v AS SELECT 1` parses.
- [x] `CREATE MASKING POLICY p WITH (...) USING (...)` parses or fails with explicit unsupported detail.
- [x] `ATTACH MASKING POLICY p ON t(c) TO ROLE r` parses or fails with explicit unsupported detail.
- [x] `CREATE RLS POLICY p WITH (...) USING (...)` parses or fails with explicit unsupported detail.
- [x] `CREATE MODEL m FROM (...) TARGET y FUNCTION f IAM_ROLE DEFAULT` parses or fails with explicit unsupported detail.

## Phase 3: Bytebase Runtime Surfaces

### Diagnostics And Ranges

- [x] `Diagnose` returns no diagnostics for valid P0 Redshift SQL.
- [x] `Diagnose` returns a syntax diagnostic with line and column for invalid SQL.
- [x] `StatementRanges` returns UTF-16 LSP ranges for single-line SQL.
- [x] `StatementRanges` returns UTF-16 LSP ranges for multi-line SQL.
- [x] `StatementRanges` handles non-BMP characters before statement starts.

### Statement Types And Validation

- [x] `GetStatementTypes` classifies CREATE TABLE.
- [x] `GetStatementTypes` classifies ALTER TABLE.
- [x] `GetStatementTypes` classifies DROP TABLE.
- [x] `GetStatementTypes` classifies INSERT, UPDATE, DELETE, and MERGE as DML.
- [x] `ValidateSQLForEditor` allows SELECT.
- [x] `ValidateSQLForEditor` allows plain EXPLAIN.
- [x] `ValidateSQLForEditor` allows SHOW statements.
- [x] `ValidateSQLForEditor` rejects DML for read-only execution.
- [x] `ValidateSQLForEditor` rejects DDL for read-only execution.
- [x] `ValidateSQLForEditor` treats EXPLAIN ANALYZE non-SELECT conservatively.

### Changed Resources

- [x] `ExtractChangedResources` reports created tables.
- [x] `ExtractChangedResources` reports dropped tables as affected resources.
- [x] `ExtractChangedResources` reports altered tables as affected resources.
- [x] `ExtractChangedResources` counts INSERT statements.
- [x] `ExtractChangedResources` counts UPDATE statements.
- [x] `ExtractChangedResources` counts DELETE statements.
- [x] `ExtractChangedResources` respects `SET search_path`.

## Phase 4: Query Span

### SELECT Lineage

- [x] Query span reports source columns for simple table columns.
- [x] Query span expands `SELECT *` from table metadata.
- [x] Query span expands table-qualified star.
- [x] Query span tracks aliases in SELECT list.
- [x] Query span tracks source columns inside expressions.
- [x] Query span tracks predicate columns from WHERE.
- [x] Query span tracks predicate columns from JOIN ON.
- [x] Query span handles JOIN USING.
- [x] Query span handles CTEs with explicit column aliases.
- [x] Query span handles CTEs without explicit column aliases.
- [x] Query span handles subquery table aliases.
- [x] Query span handles set operations.
- [x] Query span handles view definitions from metadata.

### Redshift SELECT Extensions

- [x] Query span handles `QUALIFY` as predicate context.
- [x] Query span handles `EXCLUDE (...)` result shaping.
- [x] Query span handles `SELECT INTO` as DML/DDL-like statement where appropriate.
- [x] Query span explicitly rejects unsupported `CONNECT BY` lineage instead of returning wrong lineage.

## Phase 5: Completion

### Core Completion

- [x] Completion suggests top-level SELECT/INSERT/UPDATE/DELETE/MERGE/CREATE/ALTER/DROP/COPY/UNLOAD/SHOW.
- [x] Completion suggests schema names from metadata.
- [x] Completion suggests table names from selected schema.
- [x] Completion suggests columns after `SELECT ` with FROM context.
- [x] Completion suggests columns after table alias dot.
- [x] Completion suggests CTE names in FROM.
- [x] Completion suggests CTE columns when aliases are explicit.
- [x] Completion suggests subquery alias/output columns from syntax-derived target lists.

### Redshift Slots

- [x] Completion suggests `DISTKEY`, `SORTKEY`, `DISTSTYLE`, and `ENCODE` in CREATE TABLE option context.
- [x] Completion suggests `IAM_ROLE`, `CREDENTIALS`, and format options in COPY context.
- [x] Completion suggests `IAM_ROLE`, `MANIFEST`, `HEADER`, and format options in UNLOAD context.
- [x] Completion suggests SHOW subcommands.
- [x] Completion does not invent metadata columns for unsupported shapes.

## Phase 6: Bytebase Switch

- [ ] Bytebase Redshift parser registration delegates parse/split/diagnose/ranges/types to Omni.
- [ ] Bytebase Redshift query validator delegates to Omni.
- [ ] Bytebase Redshift changed resources delegates to Omni.
- [ ] Bytebase Redshift query span delegates to Omni.
- [ ] Bytebase Redshift completion delegates to Omni.
- [ ] Bytebase no longer imports `github.com/bytebase/parser/redshift` on the Redshift main path.
- [ ] Bytebase focused Redshift parser tests pass.
- [ ] Bytebase SQL Editor validation tests pass for Redshift.
- [ ] Bytebase masking/query-span tests pass for Redshift.
- [ ] Bytebase statement report tests pass for Redshift.

## Phase 7: Omni Compatibility Verification

### AWS Command Matrix

- [x] AWS Redshift SQL command reference is represented by an Omni-side manifest.
- [x] Every manifest command has an explicit status: runtime-supported, parse-supported, explicitly unsupported, or not relevant.
- [x] Runtime-supported command samples parse successfully.
- [x] Parse-supported command samples parse successfully.
- [x] Explicitly unsupported command samples fail instead of silently producing a misleading supported result.
- [x] The matrix includes Redshift-specific commands such as `ALTER TABLE APPEND`, `ANALYZE COMPRESSION`, `COPY`, `UNLOAD`, `SHOW EXTERNAL TABLE`, `USE`, and `VACUUM`.

### Legacy Corpus Tracking

- [x] The 115-file legacy Redshift corpus is counted by a compatibility report.
- [x] Expected-failure legacy files are counted separately from passing files.
- [x] New legacy corpus failures fail the compatibility test.
- [x] Legacy expected failures that start parsing are reported as promoted files and must be reclassified.
- [x] Legacy corpus files are split into individual statements for old-parser-vs-Omni parity statistics.
- [x] Statement-level gaps are reported with examples so the next support tranche can be prioritized.

### Runtime Semantics

- [x] Compatibility verification checks Redshift CREATE TABLE options through `Parse`.
- [x] Compatibility verification checks syntax diagnostics on invalid SQL.
- [x] Compatibility verification checks UTF-16 statement ranges on multi-statement SQL.
- [x] Compatibility verification checks Redshift command statement classification for COPY, UNLOAD, MERGE, and SHOW.
- [x] Compatibility verification checks SQL Editor read-only validation for EXPLAIN and DML rejection.
- [x] Compatibility verification checks changed-resource extraction for DDL and DML.
- [x] Compatibility verification checks parser-native completion scope for FROM relations.

### Reference Redshift Harness

- [x] Compatibility verification has an optional Redshift reference execution harness.
- [x] The harness is disabled with an explicit skip reason when `REDSHIFT_COMPAT_DSN` is not set.
- [x] The report records whether reference Redshift validation ran or was skipped.

### Compatibility Report

- [x] Omni can generate a Markdown Redshift compatibility report from the command manifest and legacy corpus.
- [x] The checked-in compatibility report is tested for freshness.
- [x] The report exposes command-level parse status, legacy file counts, legacy statement parity counts, runtime semantic counts, and reference Redshift status.
